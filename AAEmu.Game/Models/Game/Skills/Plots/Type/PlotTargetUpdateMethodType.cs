namespace AAEmu.Game.Models.Game.Skills.Plots.Type
{
    public enum PlotTargetUpdateMethodType
    {
        // Original source in the skill ?
        OriginalSource = 1,
        // Original target in the skill ?
        OriginalTarget = 2,
        // Source of previous event step ?
        PreviousSource = 3,
        // Target of previous event step ?
        PreviousTarget = 4,
        Area = 5,
        RandomUnit = 6,
        RandomArea = 7
    }

    // Information about parameters :
    
    // ORIGINAL SOURCE
    // Param1 = Unknown
    // Param2 = Unknown
    // Param3 = Unknown
    // Param4 = Unknown (USED ONLY ON PLOT EVENT 1635)
    // Param5 = Unknown (NOT USED)
    // Param6 = Unknown
    // Param7 = Unknown (NOT USED)
    // Param8 = Unknown (USED ONLY ON PLOT EVENT 1635)
    // Param9 = Unknown (USED ONLY ON PLOT EVENT 1635)
    
    // ORIGINAL TARGET
    
    // PREVIOUS SOURCE
    
    // PREVIOUS TARGET
    
    // AREA
    
    // RANDOM UNIT
    
    // RANDOM AREA
}
